# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment variables (NEVER commit your real .env — it contains your API key)
.env
.env.local
.env.*.local

# SQLite database — back this up separately, don't commit it to git
prisma/dev.db
prisma/dev.db-journal

# TypeScript build info
tsconfig.tsbuildinfo

# OS files
.DS_Store
Thumbs.db
